@model IEnumerable<BecasITT.Models.Alumno>

@{
    ViewBag.Title = "Alumno";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Beca.NombreBeca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Noctrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CorreoIns)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contraseña)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Beca.NombreBeca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Noctrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CorreoIns)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contraseña)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
